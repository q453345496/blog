<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xian.blog.dao.JournalCategoryDao">

	<resultMap type="JournalCategory" id="JournalCategoryResult">
		<id property="id" column="id" />
	</resultMap>
	
	<sql id="sql_where">
		<where>
			<if test="name != null and name != '' ">
				AND name LIKE CONCAT('%', #{name}, '%')
			</if>
			<if test="rank != null">
				AND rank = #{rank}
			</if>
			<if test="parentId != null">
				AND parent_id = #{parentId}
			</if>
			<if test="isParent != null">
				AND is_parent = #{isParent}
			</if>
		</where>
	</sql>
	
	<select id="list" parameterType="Map" resultMap="JournalCategoryResult">
		SELECT * FROM t_journal_category
		<include refid="sql_where"></include>
		ORDER BY rank, name
		<if test="start != null and size != null">
			LIMIT #{start}, #{size}
		</if>

	</select>

	<select id="getTotal" parameterType="Map" resultType="Integer">
		SELECT COUNT(1) FROM t_journal_category
		<include refid="sql_where"></include>
	</select>

	<insert id="save" parameterType="JournalCategory" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO t_journal_category (name, parent_id, is_parent, rank) VALUES(#{name}, #{parentId}, #{isParent}, #{rank})
	</insert>

	<update id="update" parameterType="JournalCategory">
		UPDATE t_journal_category
		<set>
			<if test="name != null and name != '' ">
				name = #{name},
			</if>
			<if test="parentId != null">
				parent_id = #{parentId},
			</if>
			<if test="isParent != null">
				is_parent = #{isParent},
			</if>
			<if test="rank != null">
				rank = #{rank},
			</if>
		</set>
		WHERE id = #{id}
	</update>

	<delete id="delete" parameterType="Long">
		DELETE FROM t_journal_category WHERE id = #{id}
	</delete>
	
	<select id="get" parameterType="Long" resultMap="JournalCategoryResult">
		SELECT * FROM t_journal_category
		WHERE id = #{id}
	</select>
	
	<select id="getSubCount" parameterType="Long" resultType="Integer">
		SELECT COUNT(1) FROM t_journal_category
		WHERE parent_id = #{id}
	</select>
	
</mapper> 