jQuery outerWidth() 方法

```
$(selector).outerWidth(includeMargin) //默认false。不包含 margin

width() - 设置或返回元素的宽度
height() - 设置或返回元素的高度
innerWidth() - 返回元素的宽度（包含 padding）
innerHeight() - 返回元素的高度（包含 padding）
outerHeight() - 返回元素的高度（包含 padding 和 border）
outerWidth() - 返回元素的高度（包含 padding 和 border）
```